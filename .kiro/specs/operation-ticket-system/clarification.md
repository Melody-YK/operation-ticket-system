# 澄清日志：操作票管理系统

**功能**：operation-ticket-system
**会话日期**：2026-05-13
**总问题数**：1
**状态**：进行中（8个问题已回答，可继续或结束）

---

## Q1：移动端形态选择

**分类**：功能范围（Functional Scope）

**问题**：
PRD 中多处提到"PC端/移动端"发起建票、操作执行，但未明确说明移动端的具体形态。这会直接影响技术架构选型和前端实现方案。请问采用哪种移动端方案？

**提供的选项**：
- **A**：原生移动 App（iOS + Android 各一套，通过应用商店发布，支持离线能力更强）
- **B**：移动端响应式 Web（同一套代码自适应 PC/移动端，通过浏览器访问，无需安装）
- **C**：混合模式（核心操作功能用原生 App 实现，查询浏览类用 Web 页面）
- **D**：PWA（渐进式 Web 应用，Web 技术打包，可安装到桌面，支持部分离线能力）

**推荐**：选项 B - 移动端响应式 Web，技术成本低，维护简单，一套代码覆盖所有终端

**用户选择**：B（移动端响应式 Web）

**解决方案**：
- 确认全系统采用响应式 Web 架构，PC/移动端使用同一套代码
- 补充 NFR-C03：移动端适配要求（操作执行页面完整可用，触屏交互适配）
- 更新 US-001 故事描述，明确提及"同一套响应式Web系统"
- 后续所有涉及移动端的描述均按此方案对齐

**应用范围**：
- NFR 兼容性需求 → 新增 NFR-C03
- US-001 用户故事 → 更新描述

---

## Q2：并发审核冲突处理

**分类**：边角情况（Edge Cases）

**问题**：
操作票三级审核涉及多个审核人，PRD 未明确说明并发场景的处理方式。例如：同一审核级别的两个人同时打开同一张操作票并各自提交审核结果，系统应如何处理？

**提供的选项**：
- **A**：先到先得（谁先提交结果谁生效，后提交的提示"操作票状态已变更，请刷新"）
- **B**：先到先得+审核锁定（审核人打开页面即锁定操作票，其他人看到"XX正在审核"提示）
- **C**：最终覆盖（后提交的结果覆盖先提交的）
- **D**：所有审核人均需审核（同一级别的所有人均需审核通过才算通过）

**推荐**：选项 B - 先到先得+审核锁定，避免多人同时操作导致的混乱

**用户选择**：B（先到先得+审核锁定）

**解决方案**：
- 在 UC-003（审核操作票）的备选流程中补充并发锁定机制
- 增加业务规则：审核人打开审核页面时锁定操作票，其他人在锁定期间看到"XX正在审核"提示
- 锁定超时机制：若审核人打开页面后30分钟内无操作，自动释放锁定

**应用范围**：
- UC-003 审核操作票用例 → 新增备选流程 A3（并发锁定）
- 业务规则 → 新增 BR-006（审核锁定规则）

---

## Q3：角色互斥规则

**分类**：领域与数据（Domain & Data）

**问题**：
一个人是否可以同时担任操作人、监护人、批准人、发令人中的多个角色？这影响人员管理数据库设计（四表合一还是分表）和权限校验逻辑。

**提供的选项**：
- **A**：允许一人多角色，但同一张票中角色互斥（可以同时有操作人和监护人资质，但不能审核自己创建的操作票）
- **B**：严格角色互斥（一个人只能拥有一个角色，不同角色需要不同人员账号）
- **C**：完全灵活（一人可多角色，且在同一张票中也可兼任）

**推荐**：选项 A - 允许一人多角色，但同一张票中角色互斥，更灵活且避免自我审批

**用户选择**：B（严格角色互斥）

**解决方案**：
- 确认四类人员实体保持独立设计
- 数据模型中假设 A-002 已确认（严格互斥）
- 人员信息管理按四类角色分别维护

**应用范围**：
- data-model.md → 假设 A-002 已验证为 Yes

---

## Q4：待办通知方式

**分类**：集成与外部依赖（Integration）

**问题**：
PRD 提到系统在审核节点"推送待办提醒"给对应审核人。但未明确说明通知的具体方式。这影响系统是否需要集成短信、邮件、即时通讯等外部服务。

**提供的选项**：
- **A**：仅系统内通知（登录系统后在待办中心看到，不推送到外部）
- **B**：系统内通知 + 企业微信/行云推送（系统内待办中心+通过企业微信或三峡行云发消息提醒）
- **C**：系统内通知 + 短信（系统内待办中心 + 短信通知）
- **D**：全渠道推送（系统内通知 + 企业微信 + 短信，用户可选）

**推荐**：选项 B - 系统内通知 + 企业微信/行云推送，确保及时触达

**用户选择**：A（仅系统内通知）

**解决方案**：
- 待办提醒仅通过系统内部的待办中心实现
- 用户登录后在工作台看到待办事项列表
- 待办中心支持红点标记和消息列表
- 审核人需要定期登录系统查看待办（或系统可通过浏览器通知API推送桌面通知）

**应用范围**：
- UC-003 审核操作票 → 待办提醒方式修改为系统内待办中心
- 消除对外部推送服务（短信/企业微信）的依赖

---

## Q5：范围边界确认

**分类**：功能范围（Functional Scope）

**问题**：
PRD 详细描述了操作票管理系统的功能，但没有明确说明哪些功能不在当前范围内。明确边界有助于避免范围蔓延。

**提供的选项**：
- **A**：工作票管理系统（操作票关联的工作票管理、创建、流转不在本系统范围内）
- **B**：移动App原生功能（已确认采用响应式Web，因此无需开发原生移动App）
- **C**：AI智能分析（如自动识别危险源、操作步骤智能推荐等）
- **D**：以上全部（A + B + C 全部不在当前范围）
- **E**：部分自定义

**推荐**：选项 D - 以上全部
**用户选择**：B（移动App原生功能）

**解决方案**：
- 明确将"移动App原生功能"标记为不在当前范围（已采用响应式Web）
- 工作票管理系统和AI智能分析未明确排除，但也不在当前MVP中

**应用范围**：
- requirements.md → 确认技术方案为响应式Web

---

## Q6：音视频数据存储策略

**分类**：领域与数据（Domain & Data）

**问题**：
操作执行过程中需同步采集音视频、标示、设备状态转换数据，但未明确音视频数据的存储位置、保留时长和文件大小限制。这影响存储架构设计。

**提供的选项**：
- **A**：数据库BLOB存储 + 永久保留（所有音视频存入数据库，不设保留期限）
- **B**：对象存储（OSS/S3）+ 90天保留（音视频存入对象存储，90天后自动清理）
- **C**：对象存储 + 与操作票同生命周期（音视频随操作票归档后永久保留）
- **D**：本地文件服务器 + 30天短期保留

**推荐**：选项 B - 对象存储 + 90天保留，性价比高且易于扩展
**用户选择**：A（数据库BLOB存储 + 永久保留）

**解决方案**：
- 音视频数据以BLOB形式存储在数据库中
- 不设自动清理机制，随操作票永久保留
- 需关注数据库存储容量增长，建议定期监控

**应用范围**：
- data-model.md → 电子操作票实体中 media_url 字段可能需要调整为BLOB类型或保留URL引用+实际BLOB存储

---

## Q7：合规性与行业标准

**分类**：非功能性需求（Non-Functional）

**问题**：
操作票管理系统用于电力现场作业，PRD 未明确是否需满足特定电力行业合规标准或监管要求。

**提供的选项**：
- **A**：遵循电力行业通用操作票规范（满足"两票三制"等基本行业要求，无需额外认证）
- **B**：需要满足等保2.0二级要求（信息安全等级保护二级）
- **C**：需要满足等保2.0三级要求（更高安全要求）
- **D**：暂不确定，按通用系统设计

**推荐**：选项 A - 以行业通用规范为主，避免过度设计
**用户选择**：A（遵循电力行业通用操作票规范）

**解决方案**：
- 系统设计需满足"两票三制"中操作票相关要求
- 无需额外信息安全等级保护认证
- 操作票的建立、审核、执行、归档流程需符合电力行业通用标准

**应用范围**：
- NFR 合规性 → 补充行业规范要求

---

## Q8：操作票暂存与自动保存

**分类**：交互与用户体验（Interaction & UX）

**问题**：
PRD 提到操作票建立时"支持信息暂存"，但未明确暂存功能的保留策略和自动保存行为。

**提供的选项**：
- **A**：仅手动暂存（操作人手动点击"暂存"按钮才保存）
- **B**：手动暂存 + 自动保存草稿（每60秒自动保存，意外关闭可恢复）
- **C**：全自动实时保存（每输入一个字段即自动保存，无需手动操作）
- **D**：仅提交时保存（不支持暂存，离开提示未保存）

**推荐**：选项 B - 手动暂存 + 自动保存，平衡用户控制与数据安全
**用户选择**：C（全自动实时保存）

**解决方案**：
- 操作票表单每输入/修改一个字段即自动保存至服务器
- 无需手动"暂存"按钮，表单始终处于保存状态
- 网络中断时本地缓存，网络恢复后自动同步
- 关闭页面或切换设备后重新打开，自动恢复上次编辑内容

**应用范围**：
- US-001 创建操作票 → 暂存行为明确为全自动实时保存
- 简化表单交互设计，去除"暂存"按钮
